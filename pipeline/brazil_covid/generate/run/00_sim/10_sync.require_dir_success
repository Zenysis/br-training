#!/bin/bash -eu
set -o pipefail

source "${PIPELINE_UTILS_DIR}/bash/common.sh"

# TODO(abby): Uncomment when there's a good sense of the min size of this
# directory.
# AssertMinDirectorySize "${PIPELINE_FEED_DIR}" '140000'

# Sync entire directory to minio
UploadDirectory "${PIPELINE_FEED_DIR}" 's3/zenysis-br-covid/sim'
